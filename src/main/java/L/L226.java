package L;

import javax.swing.tree.TreeNode;

public class L226 {


    public void invertTree(TreeNode root) {
/*
        if (root == null) return null;
        X.TreeNode node = new X.TreeNode(root.val);
        node.right = invertTree(root.left);
        node.val = root.val;
        node.left = invertTree(root.right);
        return node;
        */

    }
}
